lookup_options:
  classifier::extra_classes:
    merge:
      strategy: deep
      knockout_prefix: --
      sort_merge_arrays: true
      unpack_arrays: ","
  classifier::rules:
    merge:
      strategy: deep
